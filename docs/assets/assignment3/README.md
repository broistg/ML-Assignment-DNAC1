Assignment 3 assets — image filenames and instructions

Paste the images you want shown on the Assignment 3 page into this folder.
Recommended filenames (the HTML page uses these names):

- `assignment3_samples.png`      # Sample grid of images (recommended: 800×400 or similar)
- `assignment3_label_dist.png`   # Label distribution / bar chart (recommended: 800×400)
- `assignment3_confusion.png`    # Confusion matrix (recommended: 800×600)

Optional visuals you can also add (recommended names):
- `assignment3_training_curve.png`   # Training accuracy / loss curves
- `assignment3_tsne.png`            # t-SNE / UMAP visualization of features
- `assignment3_comparison.png`      # Bar chart comparing feature extractors × classifiers

Notes:
- Image formats supported: PNG, JPG. Prefer PNG for crisp plots and transparency.
- Sizes: keep width <= 1200px for best layout; smaller thumbnail sizes (e.g., 800px wide) are fine.
- After adding images, open `docs/assignment3.html` in a browser and refresh to view them.

If you want me to add more placeholders or tune layout (e.g., add captions, icons, or lightbox), tell me which images you'll paste and I'll update the page accordingly.
